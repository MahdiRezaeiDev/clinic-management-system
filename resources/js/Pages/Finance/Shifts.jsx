import {ClinicHeader,ClinicPage,ClinicPanel,ClinicStat,fieldClass,primaryButton} from '@/Components/ClinicUI';import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';import {Head,useForm} from '@inertiajs/react';import {CircleDollarSign,LockKeyhole,Play,Scale,WalletCards} from 'lucide-react';
const money=v=>`${Number(v||0).toLocaleString()} ؋`;
export default function Shifts({activeShift,shifts}){const open=useForm({opening_amount:'',notes:''});const close=useForm({counted_amount:'',notes:''});const credit=activeShift?.transactions.filter(t=>t.direction==='credit'&&!t.voided_at).reduce((s,t)=>s+Number(t.amount),0)||0;const debit=activeShift?.transactions.filter(t=>t.direction==='debit'&&!t.voided_at).reduce((s,t)=>s+Number(t.amount),0)||0;const expected=Number(activeShift?.opening_amount||0)+credit-debit;return <AuthenticatedLayout title="شیفت صندوق"><Head title="شیفت صندوق"/><ClinicPage className="space-y-6"><ClinicHeader title="مدیریت شیفت صندوق" subtitle="کنترل موجودی آغاز، گردش نقدی و اختلاف پایان شیفت" icon={WalletCards}/>{activeShift?<><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><ClinicStat title="موجودی آغاز" value={money(activeShift.opening_amount)} icon={Play}/><ClinicStat title="دریافتی شیفت" value={money(credit)} icon={CircleDollarSign} color="green"/><ClinicStat title="پرداختی شیفت" value={money(debit)} icon={CircleDollarSign} color="red"/><ClinicStat title="موجودی مورد انتظار" value={money(expected)} icon={Scale} color="blue"/></div><ClinicPanel title="بستن شیفت" icon={LockKeyhole}><form className="grid items-end gap-5 md:grid-cols-3" onSubmit={e=>{e.preventDefault();close.patch(route('finance.shifts.close',activeShift.id))}}><Field label="موجودی شمارش‌شده"><input type="number" className={fieldClass} value={close.data.counted_amount} onChange={e=>close.setData('counted_amount',e.target.value)}/></Field><Field label="یادداشت"><input className={fieldClass} value={close.data.notes} onChange={e=>close.setData('notes',e.target.value)}/></Field><button className={primaryButton}>بستن و تسویه شیفت</button></form></ClinicPanel></>:<ClinicPanel title="بازکردن شیفت جدید" icon={Play}><form className="grid items-end gap-5 md:grid-cols-3" onSubmit={e=>{e.preventDefault();open.post(route('finance.shifts.open'))}}><Field label="موجودی آغاز"><input type="number" className={fieldClass} value={open.data.opening_amount} onChange={e=>open.setData('opening_amount',e.target.value)}/></Field><Field label="یادداشت"><input className={fieldClass} value={open.data.notes} onChange={e=>open.setData('notes',e.target.value)}/></Field><button className={primaryButton}>شروع شیفت</button></form></ClinicPanel>}<ClinicPanel title="تاریخچه شیفت‌ها" icon={WalletCards} bodyClassName="p-0"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-teal-700 text-white"><tr>{['صندوق‌دار','آغاز','انتظار','شمارش','اختلاف','وضعیت'].map(h=><th key={h} className="px-5 py-4 text-right">{h}</th>)}</tr></thead><tbody>{shifts.map(s=><tr key={s.id} className="border-b"><td className="px-5 py-4 font-bold">{s.user?.name}</td><td className="px-5 py-4">{money(s.opening_amount)}</td><td className="px-5 py-4">{money(s.expected_amount)}</td><td className="px-5 py-4">{money(s.counted_amount)}</td><td className={`px-5 py-4 font-bold ${Number(s.discrepancy)<0?'text-red-600':'text-green-600'}`}>{s.discrepancy==null?'-':money(s.discrepancy)}</td><td className="px-5 py-4">{s.closed_at?'بسته':'باز'}</td></tr>)}</tbody></table></div></ClinicPanel></ClinicPage></AuthenticatedLayout>}
const Field=({label,children})=><label><span className="mb-2 block text-sm font-medium text-gray-600">{label}</span>{children}</label>;
